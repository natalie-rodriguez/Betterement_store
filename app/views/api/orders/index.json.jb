@orders.map do |order|
  
  {
  id: order.id,
  subtotal: order.subtotal,
  tax: order.tax,
  total: order.total,
  user_id: order.user_id,
  product: order.product,
  quantity: order.quantity
  }
end 

  #If you want to change the return value use map.Map returns a new array with the results.



